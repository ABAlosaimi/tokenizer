## Tokenizer 
This tokenizer is one component of the project called TokiMet, which is acting as text collector and normalizer from token sources (see TokiMet repository) in the organization environment, this allows us to know the consumption of tokens in the organization and also to have a better understanding of the text data being processed. 

